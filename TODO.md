> Back-end

- Move JWT secret to application configuration // System.env
- Add login/registration to unauthenticated GQL routes
- Add phoenix chat channel + JWT verification on join
- Rename `user.name` -> `user.username`
- Add Bamboo + Mailgun
- Add email confirmation on registration
- Add password reset email


> Front-end

- Add redux to react application
- Add react storybook for isolated component development
- Add API boilerplate redux actions
- Setup GQL query + mutation folder structure
